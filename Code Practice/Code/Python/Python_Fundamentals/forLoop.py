# take array. return minimum value in the list. if list is empty, have the function rerun false.
testArray[]

def minimum(testArray)
    if len(testArray) == 0:
        print(none)
        return none
    else: 
        minValue = someArray[0]
        for i in range (0, len(testArray), 1):
            if someArray[i < minValue:
                    minValue = someArray[i]
                    
                    return minValue




